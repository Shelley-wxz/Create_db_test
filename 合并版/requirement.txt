# 核心数据处理与表格操作
pandas>=1.5.0
numpy>=1.24.0

# 网络请求与爬虫基础
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# PDF 与文档解析
PyMuPDF>=1.22.0

# 大语言模型 (LLM) 交互
openai>=1.0.0

# 辅助工具
python-dotenv>=1.0.0
tqdm>=4.65.0

# JSON 修复库：用于自动修复 LLM 生成的非法 JSON 字符串（如缺少逗号、多余逗号等语法错误）
# 当 json.loads 解析失败时，通过 repair_json() 尝试修复后重新解析，避免跳过论文数据
json_repair==0.50.1